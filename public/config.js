/* Dónde está el servidor del juego.
   Vacío: la página y el servidor viven en el mismo sitio (lo normal en Render
   con un solo servicio). Si la página se aloja aparte —para que abra de una y
   muestre «Despertando la finca» mientras el servidor gratuito arranca—, aquí
   va su dirección, por ejemplo "https://cosecha.onrender.com".
   En Render lo escribe solo configurar-frente.js al publicar la página. */
window.COSECHA_SERVIDOR = window.COSECHA_SERVIDOR || "";
